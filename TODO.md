# Todo

[ ] Implement article generation
[ ] There should be an editing stage that doesn’t involve changing the session extractions.
[ ] object references should be eliminated in favor of an unordered list of subjects; this also allows removal of references.
[ ] Investigate: does first appearance still provide value?
[ ] Add entity resolution stage to aggregation.
[ ] Make entity-session dependencies explicit by creating dependency files.
[ ] Remove sentinel files `.register_complete` and `.split_complete`.
